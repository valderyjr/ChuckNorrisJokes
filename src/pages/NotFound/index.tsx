import React from "react";
import PageTitle from "../../components/PageTitle";
import AppLayout from "../../layouts/AppLayout";

const NotFound = () => {
  return (
    <AppLayout>
      <PageTitle title="Esta página não existe." />
    </AppLayout>
  );
};

export default NotFound;
